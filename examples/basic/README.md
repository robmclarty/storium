# Basic

The simplest possible Storium example. Connects to an in-memory database, defines a single store with a few columns, and demonstrates basic CRUD operations (create, read, update, delete). Start here if you're new to Storium.
