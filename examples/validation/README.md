# Validation

Demonstrates Storium's built-in validation pipeline. Covers input transforms (trimming, lowercasing), built-in assertions (`is_email`, `not_empty`), custom assertion functions, required field enforcement, and how to handle `ValidationError` with per-field error messages.
