# Relations

Demonstrates how to model relationships between tables using Storium's composable helpers. Covers `withBelongsTo` for foreign key joins and `withMembers` for many-to-many membership patterns (e.g., users belonging to teams through a join table).
