# Migrations

Demonstrates the full migration lifecycle with Storium. Covers setting up `storium.config.ts` with `defineConfig`, generating migrations from schema changes, applying pending migrations, checking migration status, and writing seed files with `defineSeed`.
