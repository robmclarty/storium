# Memory

Demonstrates using the `memory` dialect to run Storium with an ephemeral in-memory SQLite database. Useful for quick prototyping, unit tests, and trying out Storium without setting up a database server.
