# Custom Queries

Demonstrates how to extend stores with custom query functions. Covers using the repository context (`ctx`) to compose queries from built-in CRUD operations, overriding default methods (e.g., adding password hashing to `create`), and coordinating multiple operations within a transaction using `withTransaction`.
