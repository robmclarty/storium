# PostgreSQL

Demonstrates how to connect Storium to a PostgreSQL database. Covers connection configuration via URL and individual parameters, and shows Postgres-specific column types like `uuid` and `jsonb`.
