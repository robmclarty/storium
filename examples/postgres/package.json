{
  "name": "storium-examples-postgres",
  "description": "Using Storium with a PostgreSQL database, including connection setup and Postgres-specific features like jsonb and uuid columns",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx app.ts"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  }
}
