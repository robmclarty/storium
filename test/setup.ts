/**
 * Global test setup — loaded before every test file via vitest.config.ts `setupFiles`.
 *
 * Use this file for:
 * - Global before/after hooks (e.g., database setup/teardown)
 * - Shared test utilities and custom matchers
 * - Environment configuration
 */

export const setup = { sample: 'keep' }
